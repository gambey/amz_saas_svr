# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# 允许的前端域名，多个域名用逗号分隔，使用 * 允许所有域名（不推荐生产环境）
# 注意：如果使用 www 子域名，必须同时包含带 www 和不带 www 的域名
# 示例: ALLOWED_ORIGINS=http://bestswitch2dock.xyz,https://bestswitch2dock.xyz,http://www.bestswitch2dock.xyz,https://www.bestswitch2dock.xyz
ALLOWED_ORIGINS=http://bestswitch2dock.xyz,http://www.bestswitch2dock.xyz,http://localhost:3000

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=amz_saas_db

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# 定时邮箱抓取配置
# 是否启用定时任务（true/false，默认 true）
AUTO_CRAWL_ENABLED=true
# 抓取关键词（必填）
AUTO_CRAWL_KEYWORD=velolink
# 品牌（可选，会写入 customers 表的 brand 字段）
AUTO_CRAWL_BRAND=Velolink
# 标签（可选，会写入 customers 表的 tag 字段）
AUTO_CRAWL_TAG=NS2 3in1
# 标签（可选，会写入 customers 表的 remarks 字段）
AUTO_CRWAL_REMARKS=自动
# 抓取最近N天的邮件（默认7天）
AUTO_CRAWL_DAYS_BACK=7
# Cron 表达式（默认每天早上7点北京时间，使用 Asia/Shanghai 时区）
# 格式: 秒 分 时 日 月 周
# 示例: '0 7 * * *' 表示每天7点执行
AUTO_CRAWL_CRON=0 7 * * *
# 启动时是否立即执行一次（true/false，默认 false，用于测试）
AUTO_CRAWL_RUN_ON_START=false
